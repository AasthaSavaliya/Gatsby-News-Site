/**
 * @description The initial state of the application;
 * Over the course of usage, the state may be dynamically populated/updated through APIs or dispatching (triggering) actions.
 * @summary The state in which the application should be when loading for the first time.
 */
export const AppInitialState = {

}